extend ../_component/layout/base

block config
    -
        var title= "Success"
        var description= "Explore things you did not know so far or things that you already know but for which you need a gentle reminder"
block content
    .content
      h2 Few interesting articles about success
      .list-wrapper
        each article in data.success
          include ../_component/article/article
          //- pre=JSON.stringify(article, null, 2)
